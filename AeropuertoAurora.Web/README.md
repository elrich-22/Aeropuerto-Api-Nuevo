# Aeropuerto Aurora Web

Front en React y Bootstrap para consumir el API del proyecto.

## Ejecutar

```powershell
npm install
npm run dev
```

Por defecto consume `http://localhost:5185`. Si el API usa otra URL, crea un archivo `.env` basado en `.env.example`.
